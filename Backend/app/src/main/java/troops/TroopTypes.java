package troops;

// Enumeration to store different troop types, using enums rather than the troop clases directly in the manager hashmap allows
// greater flexibility for implementation of future troop types
public enum TroopTypes {
    ARCHER, WARRIOR, MAGE, CAVALRY
}
