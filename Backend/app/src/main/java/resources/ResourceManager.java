package resources;

import java.util.HashMap;

public class ResourceManager {
    HashMap<Resource, Integer> resourceManager;

    public ResourceManager() {
        resourceManager = new HashMap<>();
    }
}
